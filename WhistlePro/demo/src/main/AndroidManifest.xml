<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.enst.pact34.whistlepro.demo"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="15" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="WhistleProDemo"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.FirstActivity"
            android:label="WhistleProDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PercussionTest" />
        <activity android:name=".activities.NameActivity" />
        <activity android:name=".activities.CorrectionActivity" />
        <activity android:name=".activities.EnregistrementActivity" />
        <activity android:name=".activities.ValidationProcActivity" />
        <activity android:name=".activities.NewTrackActivity" />
        <activity android:name=".activities.ProcessingActivity" />
        <activity android:name=".activities.InstrChoiceActivity" />
        <activity android:name=".activities.AddTrackActivity" />
        <activity android:name=".activities.KeepActivity" />
        <activity android:name=".TrackSelection" />
    </application>

</manifest>
